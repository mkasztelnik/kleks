%li.clearfix(id="conference-#{item.id}")
  .left
    = link_to_if can?(:edit, item), item.title, [:edit, item], title: item.title, remote: true do
      = link_to item.title, item, remote: true
    %small.light
      #{item.conference_name}, #{item.country_name}, #{item.year} (#{item.presentation_type_text})
  - if can? :destroy, item
    .pull-right
      =link_to item, remote: true,
                     method: :delete,
                     class: 'delete',
                     data: { confirm: t('conference.destroy_confirmation') } do
        = icon('times')