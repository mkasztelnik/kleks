%li.clearfix(id="publication-#{item.id}")
  .left
    = link_to_if can?(:edit, item), item.publication_title, [:edit, item], title: item.publication_title, remote: true do
      = link_to item.publication_title, item, remote: true
  - if can?(:destroy, item)
    .pull-right
      =link_to item, remote: true,
                     method: :delete,
                     class: 'delete',
                     data: { confirm: t('publication.destroy_confirmation') } do
        = icon('times')