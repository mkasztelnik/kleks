en:
  omniauth:
    email_missing: "It seems like you don't have email set on your %{provider} account, which is required. Please use another logging option."
  activerecord:
    errors:
      messages:
        blank: This field is required
      models:
        profile:
          attributes:
            date_of_birth:
              before: You must be at least 18 years old to participate in the Summer School
        education:
          attributes:
            end_date:
              after: End date need to be later than start date
              not_a_date: wrong date format
            start_date:
              not_a_date: wrong date format
        work:
          attributes:
            end_date:
              not_a_date: wrong date format
              after: End date need to be later than start date
            start_date:
              not_a_date: wrong date format
        conference:
          attributes:
            end_date:
              not_a_date: wrong date format
              after: End date need to be later than start date
            start_date:
              not_a_date: wrong date format
        training:
          attributes:
            end_date:
              not_a_date: wrong date format
              after: End date need to be later than start date
            start_date:
              not_a_date: wrong date format
