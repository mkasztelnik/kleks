en:
  activerecord:
    errors:
      models:
        profile:
          attributes:
            date_of_birth:
              before: You must be older than 18 years
        education:
          attributes:
            end_date:
              after: End date need to be later than start date
              not_a_date: wrong date format
            start_date:
              not_a_date: wrong date format
        work:
          attributes:
            end_date:
              not_a_date: wrong date format
              after: End date need to be later than start date
            start_date:
              not_a_date: wrong date format
        conference:
          attributes:
            end_date:
              not_a_date: wrong date format
              after: End date need to be later than start date
            start_date:
              not_a_date: wrong date format
        training:
          attributes:
            end_date:
              not_a_date: wrong date format
              after: End date need to be later than start date
            start_date:
              not_a_date: wrong date format